import { render, screen } from '@testing-library/react';
import Header from './Header';

describe('Test the header', () => {
  //   test('the header menu should display correctly', () => {
  //     render(<Header />);
  //     expect(screen.queryByText(/Quoters/)).toBeInTheDocument();
  //   });
  // Test when the currentuser is undefined so the menu should be register and login. Test this
  // Test when current user is defined and test that the menus are there
  // Test when current user is defined you can click add quote
});
